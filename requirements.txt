# Cryptographic operations
# ECDSA key generation, signing, and verification
cryptography==42.0.8

# HTTP requests
# Edge agent firmware download from GitHub Releases
requests==2.32.3

# AWS S3 integration
# Firmware upload (optional — used if S3 distribution chosen)
boto3==1.34.144

# Testing framework
pytest==8.2.2

# Test coverage reporting (optional)
pytest-cov==5.0.0